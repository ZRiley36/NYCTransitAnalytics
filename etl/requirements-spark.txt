# PySpark ETL Dependencies
pyspark==3.5.0
pyarrow>=14.0.1  # Updated to support Python 3.13 (14.0.1 has no wheels for 3.13, newer versions do)
pandas>=2.0.0  # Required for Windows workaround (converting Spark DataFrames to Parquet)

